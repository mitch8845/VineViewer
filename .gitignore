# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Gradle build output. The root /build/ rule above does not cover these.
/android/build/
/android/app/build/
/android/.gradle/

# Generated by build_runner (drift schema code).
# Deliberately not committed: a checked-in copy can fall out of sync with
# tables.dart, leaving the code and the database disagreeing about the schema.
# CI regenerates it, so a stale copy cannot exist. After cloning, run:
#   dart run build_runner build
*.g.dart

# Signing secrets — NEVER commit these.
# The release keystore is unrecoverable if lost and unrotatable if leaked.
# Back it up offline; store the CI copy in GitHub Secrets.
*.jks
*.keystore
key.properties
android/key.properties

# Claude Code machine-local settings (permissions, etc.) — not project config
.claude/settings.local.json
